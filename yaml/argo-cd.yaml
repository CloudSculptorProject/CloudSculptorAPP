apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app1
  namespace: default
spec:
  destination:
    server: 'https://aks01-dns-isxc8mbx.hcp.eastus.azmk8s.io:443'
    namespace: default
  project: default
  source:
    repoURL: 'https://github.com/CloudSculptorProject/CloudSculptorAPP.git'
    path: my-app1
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
